# Prompt: Implement One Feature

You are working in this repository.

Task:

- Read `docs/SPEC.md` and `docs/TASKS.md`.
- Implement exactly one unchecked task from `docs/TASKS.md`.
- Keep changes minimal and production-safe.

Constraints:

- Do not add unnecessary dependencies.
- Keep mobile and desktop responsive.
- Update `docs/TASKS.md` and `docs/SPEC.md` if behavior changes.

Output:

- Summary of what changed
- Files touched
- Any follow-up tasks
