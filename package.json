{
  "name": "job-apply",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "lint": "eslint main.js auth.js account.js api/*.js \"api/auth/**/*.js\" lib/*.js",
    "format:check": "prettier --check \"**/*.{js,json,md,html,css,yml,yaml}\"",
    "check": "npm run lint && npm run format:check",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.2",
    "http-server": "^14.1.1",
    "prettier": "^3.8.1",
    "vitest": "^3.2.4"
  }
}
